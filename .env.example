# MySQL 数据库根密码 (建议在本地开发时修改)
MYSQL_ROOT_PASSWORD=your_root_password_here
# 数据库名称
MYSQL_DATABASE=material_db

# 定义要创建的普通用户及其密码
MYSQL_USER=app_user
MYSQL_PASSWORD=your_app_user_password

# FastAPI 应用连接数据库的配置
# 注意：在 Docker Compose 环境中，DB_HOST 必须与服务名保持一致（通常为 db）
DB_HOST=db
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=your_app_user_password
DB_NAME=material_db